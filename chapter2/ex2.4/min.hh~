#ifndef MINING_HH
#define MINING_HH

namesapce myLib{
  int min (int a, int b);
  double min (double a, double b);
  double min (int a, double b);

  // Array of integers version
  int min (int a[], int n);
}
#endif
