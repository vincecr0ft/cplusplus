#ifndef MAXING_HH
#define MAXING_HH

namespace myLib{
  int max (int a, int b);
  double max (double a, double b);
  double max (int a, double b);

  // Array of integers version
  int max (int a[], int n);
}
#endif
